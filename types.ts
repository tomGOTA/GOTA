export interface FaqItem {
  question: string;
  answer: string;
}

// Fix: Add and export the Testimonial interface.
export interface Testimonial {
  quote: string;
  name: string;
  role: string;
  avatarUrl: string;
}
